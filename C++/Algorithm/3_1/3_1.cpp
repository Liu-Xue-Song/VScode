#include <iostream>
using namespace std;
int search(int *a, int k, int end, int n)
{
    if (end - k < 2)
        return a[k];
    // else
    //     while
}
int main()
{
    int n;
    cin >> n;
    int *a = new int[n];
    for (int i = 0; i < n; i++)
        cin >> a[i];
}